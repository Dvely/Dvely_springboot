# 미완료 작업 — 2026-09-25 기준 스냅샷

> **이 파일은 스냅샷이다.** 작업 순서·우선순위의 SSOT 는 `.notion/ROADMAP.md` 의 "1. 현재 상태"이고,
> 개별 작업은 GitHub Issue 가 정본이다. 이 파일은 그 둘이 다루지 않는 것 — **무엇이 누구 손에 막혀
> 있는가** — 를 한눈에 보려고 만들었다.
>
> 오래된 문서가 끝난 일을 가리키면 다음 사람이 그것을 다시 판다(이번 세션에 실제로 네 번 있었다).
> 그래서 항목마다 **어떻게 확인하는지**를 같이 적었다. 확인해서 끝난 것은 지운다.

---

## 0. 릴리스 현황 — 2026-09-25 12:26 시점 `develop == main`

미릴리스 커밋 없음. `#344` 네 항목과 템플릿 썸네일 필드가 PR #389(`a070cf2`)로 운영에 나갔다.
기동 12:26:19 · ERROR 0 · Flyway V64 · `thumbnailUrl` 운영 응답에 17/17 확인.

**확인 방법**: `git log --oneline origin/main..origin/develop` — 비어 있으면 미릴리스 없음.

> **여기서 한 번 실수했으니 남긴다.** `#344` 를 네 PR 로 쪼개 develop 에 머지하면서 **릴리스를 이틀 가까이
> 끊지 않았다.** 그 사이 과금이 걸린 수정 둘(ECR lifecycle · EIP release)이 develop 에만 있었고, develop 에
> 있는 동안은 **고친 효과가 0이다.** 비용·보안 성격의 수정은 develop 머지가 아니라 **운영 배포가 완료**여야
> 한다. PR 을 쪼갤 때는 마지막에 릴리스를 함께 계획한다.

---

## 1. 사람만 할 수 있는 것

### 1-1. Let's Encrypt 인증서 — **완료 (2026-09-25)**

```
issuer=Let's Encrypt CN=YE2 · subject=CN=qeploy.com · 만료 2026-12-24
nginx qeploy.com:443 블록이 /etc/letsencrypt/live/qeploy.com/ 을 쓴다
certbot renew --dry-run  →  성공
deploy hook  /etc/letsencrypt/renewal-hooks/deploy/reload-nginx.sh  설치
```

**성공 판정**: 검증 켠 상태로 오리진에 HTTPS 가 붙는다(`--resolve` 로 3/3 200). 전에는 `code=000` 이었다.
Cloudflare 경유도 교체 뒤 그대로 정상(5/5).

**이제 가능해진 것**: Cloudflare 가 불안정할 때 **DNS 를 grey cloud 로 돌려 우회**할 수 있다. 전에는
오리진 인증서가 Cloudflare Origin CA 라 그렇게 하면 전 사용자 HTTPS 가 깨졌다. orange cloud 로 두어도
무해하다(Full/strict 가 공개 신뢰 인증서를 그대로 받는다).

**되돌리려면** `ssl_certificate` 두 줄을 `/etc/ssl/cloudflare/dvely-origin.{pem,key}` 로 바꾸면 된다 —
옛 인증서는 지우지 않았다. 백업: `dvely.bak-swap-20260925-202903`.

> **여기서 두 가지를 배웠다.**
>
> **① 장애가 자기 해결책을 막는다.** 첫 발급 시도가 **522** 로 실패했다 — Cloudflare 가 오리진에 닿지
> 못해서, HTTP-01 챌린지도 닿지 못했다. 엣지가 불안정할 때 쓰려고 만드는 우회로를, 바로 그 불안정
> 때문에 만들 수 없었다. 경로가 건강한 창을 확인한 뒤에야 성공했다. **비상 대비는 평시에 해 둬야 한다.**
>
> **② `server` 직속 `return` 은 location 매칭보다 먼저 실행된다.** 80 포트 블록이 이랬는데
> ACME location 을 통째로 삼켰다(평문 HTTP 챌린지가 301). `return` 은 rewrite 단계라 location 선택보다
> 앞선다. `location /` 로 내려야 `^~` 접두사 매칭이 이긴다. **이 수정은 90일마다 도는 자동 갱신에도
> 필요하다** — 안 고쳤으면 갱신이 조용히 실패했을 것이다.
>
> 만료 알림은 받지 않는다(이메일 없이 등록). 필요하면 `certbot update_account -m <메일>`.

### 1-2. Cloudflare — 이번에는 건드리지 않기로 했다

사용자 판단으로 제외했다. 기록만 남긴다.

- **Caching → Tiered Cache** 켜기(무료). 엣지가 오리진에 직접 붙는 대신 상위 계층을 거쳐 왕복과
  불안정 노출이 줄어든다
- 장애 공지 확인(`status.cloudflare.com`, zone Overview)
- (유료) Argo Smart Routing — 엣지→오리진 경로 최적화

### 1-3. 오리진 리전 — 근본 원인

**사용자는 한국인데 오리진이 싱가포르(`13.251.154.173`, ap-southeast-1)다.** 이틀간 관측한 모든
지연·실패가 이 구조에서 나왔다.

```
2026-09-23 아침  HKG  0.120s / NRT 0.223s   정상
2026-09-23 밤    SEA  0.32s
2026-09-24 낮    LAX  평균 1.04s (최대 2.95s)
2026-09-25 아침  ATL  2.80s · 실패율 67%
2026-09-25 낮    SJC  0.35s · 실패율 0%
```

같은 순간 `cloudflare.com` 은 ICN(서울)을 주고 오리진 직접은 5/5 가 0.23~0.31s 로 평평하다 —
**우리 코드도 서버도 아니다.** 서울(`ap-northeast-2`)로 옮기면 엣지가 어디에 붙든 거리가 줄고 ICN 엣지에
붙을 확률도 올라간다. 이전 비용·다운타임은 별도 판단이 필요하다.

---

## 2. 코드로 남은 것

### 2-1. `#344` 9-2 — SDK 클라이언트 캐시 (과금 아님)

**#344 의 과금 항목(9-3 ECR · 9-4 EIP)은 끝났다.** 남은 9-2 는 지연·커넥션 설정이다.

제안은 "연결별 클라이언트 캐시 + 잦은 경로는 `ApacheHttpClient`" 인데, **클라이언트만 캐시하면 얻는
것이 거의 없다** — 현재 `UrlConnectionHttpClient` 는 커넥션 풀이 없어서 객체를 재사용해도 요청마다
TCP+TLS 를 새로 맺는다. 실제 비용은 그쪽이고 클라이언트 생성(로컬 작업)은 아니다.

값을 내려면:
- `apache-client` 는 **선언된 의존성이 아니다**(`build.gradle` 에 `url-connection-client` 만) — 추가 필요
- `UrlConnectionHttpClient` 가 **12개 클래스**에 퍼져 있다
- 클라이언트를 캐시하면 수명 관리가 따라온다(9-1 이 provider 하나에 대해 한 것을 다섯 종류에)

**별 단위로 보는 것이 맞다.**

### 2-2. `#332` — JAVA_FULLSTACK 프리뷰 검증

완료 기준 6개 중 **5개는 코드에 있다**(`ContainerRole`, `PREVIEW_USER=node`,
`prepareWorkspaceOwnership`, `installPackages`, egress 정책). 남은 하나가 **JAVA_FULLSTACK 프리뷰가
정상 동작하는지**다.

**`docker run` 수동 테스트로 대신하면 안 된다.** 이번 세션에 정확히 그 방식이 거짓 통과를 줬다 —
모든 파일이 root 소유라 `--no-same-owner` 문제를 못 잡았고, 실제 파이프라인에서만 드러났다.
실제 에이전트 실행이 필요하고, 그러려면 BYOK 키와 화면을 몰 사람이 있어야 한다.

### 2-3. `#318` — 템플릿 갤러리 (BE 끝, FE 몫)

BE 범위는 끝났다. 카탈로그 API·씨딩·`templateType` 검증·`thumbnailUrl` 전부 운영 또는 develop 에 있고,
썸네일 17종이 Pages 에 발행돼 `image/jpeg` 로 서빙된다(실측: 19~37KB, 640x400, 확장자와 형식 일치).

**남은 것은 FE 갤러리 화면 하나**다. 이슈를 BE 기준으로 닫고 FE 를 별건으로 떼는 것을 제안해 뒀다
(이슈 코멘트에 항목별 근거를 달아 놓음).

선택 항목: 데모 이미지가 17종 합쳐 **24MB** 이고 그중 **79개 `.png` 가 실제로는 JPEG** 다. 저장소가
`Dvely/qeploy-templates` 라 이 이슈 범위 밖으로 볼 수도 있다.

### 2-4. `#154` — 관리형 서브도메인 인증서

구조적 문제다. Cloudflare 프록시 때문에 GitHub Pages 가 인증서를 발급받지 못한다. **프록시를 끌지,
도메인 전략을 바꿀지가 먼저 결정돼야** 착수할 수 있다.

### 2-5. 별건으로 남긴 판단 — `REPOSITORY_BINDING` 승인 취소 의미

`#380` 검증 중 실물로 드러났다. 프리뷰가 회수되면 스윕이 승인을 닫으면서 태스크를 `CANCELLED` 로
만드는데, `REPOSITORY_BINDING` 승인은 **사용자가 거절해도 태스크를 취소하지 않는다**(작업은 성공으로
끝나고 저장소만 안 만든다). 즉 CODE 작업 자체는 성공했는데 태스크가 취소로 기록된다.

기존 `abandonStaleApprovalTask`(7일 스윕)도 두 종류를 구분하지 않아 일관성은 맞췄지만, 구분이 필요한지
별건으로 볼 값어치가 있다.

---

## 3. 검증 안 된 것

코드는 들어갔고 테스트는 통과했지만 **실제로 확인하지 못한** 것들. 여기에 적는 이유는, 검증되지 않은
것을 검증된 것처럼 기억하는 일이 이번 세션에 실제로 있었기 때문이다.

| | 왜 못 했나 |
|---|---|
| 운영에서 하트비트(`#370`) 실측 | 인증된 호출이 필요한데 운영 토큰 발급이 세션 권한 밖이다. 운영 로그로 사후 관측도 못 했다 — 배포 이후 에이전트 트래픽이 0건이다(운영 사용자 7명 중 AI 키 등록이 1명이고, 그것은 **의도된 비용 통제**다) |
| Cloudflare 가 유휴 SSE 를 5분 전에 자르는지 | 측정 스크립트만 있고 미실행. 하트비트 필요성은 로그로 이미 확정됐으니 부차적이다 |
| `#332` JAVA_FULLSTACK | 위 2-2 |
| `#344` 9-1·9-3·9-4·9-5 를 운영에서 | **애초에 운영에 배포되지 않았다**(0번 항목) |

**검증된 것**(혼동 방지): `#376` hold 는 실태스크가 6시간 5분 생존하며 `expires_at` 이 정확히 +6h 인 것으로,
`#380` 스윕은 두 태스크에서 취소·캐스케이드·사유 메시지·hold 값 렌더로, `#372` 404 는 운영에서
인증 없이 닿는 경로(`permitAll` 아래 미매핑)로 확인했다.

---

## 4. FE 쪽 (`Dvely_FE`) — 참고

FE 는 `main` 하나가 통합 브랜치이고 **머지가 곧 프로덕션 배포**다(BE 와 모델이 다르다).

오늘까지 `#106`(SSE 커서) `#107`(뒤로가기 캐시) `#108`(복원 시 진행 표시) `#110`(가짜 크레딧)
`#111`(조각 로딩 실패) `#112`(lint 차단) 이 들어갔다. 열린 PR·이슈는 0건이다.

**갤러리 착수에 필요한 BE 계약은 다 답해 뒀다:**

- 씨딩은 **첫 CODE 스텝**에서 돈다(`CodeAgentService:199`). 생성 시점엔 검증+저장만 한다
- 그래도 **`templateType` 은 생성 요청에만 실을 수 있다** — 수정 경로는 그 필드를 받지 않는다.
  생성 후에 붙일 방법이 없으므로 템플릿 선택이 `POST /projects` 에 도달해야 한다
- **`startMode: 'template'` 과 `templateType` 을 반드시 같이** 보내야 한다. `startMode: 'blank'` 이면
  `normalizeTemplateType` 이 `templateType` 을 **조용히 null 로 버린다**(오류도 없다)
- 없는 `templateType` 은 **400**(`TemplateCatalogGuard`). 카탈로그 id 만 보내야 한다
- `thumbnailUrl` 은 **nullable** 이다 — 카탈로그를 Pages 에서 실시간으로 받으므로 발행 순서에 따라
  없을 수 있다. FE 는 없으면 텍스트 카드로 떨어지게 만들기로 했다
