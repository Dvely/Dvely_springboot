package com.example.dvely.environment.domain.value;

/** Change-history event kind recorded for an {@code EnvironmentVariable} (see EnvironmentVariableHistory). */
public enum EnvironmentVariableAction {
    CREATED,
    UPDATED,
    DELETED
}
