package com.example.dvely.project.domain.value;

/** Cost estimate line-item category — one {@code resourceCosts} row per category per estimate. */
public enum CostResourceType {
    COMPUTE,
    STORAGE,
    NETWORK
}
