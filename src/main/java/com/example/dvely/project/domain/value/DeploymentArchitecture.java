package com.example.dvely.project.domain.value;

/** Provider-neutral deployment shape a project runs on (design D2). */
public enum DeploymentArchitecture {
    SERVER,
    CONTAINER,
    SERVERLESS
}
