package com.example.dvely.project.domain.value;

/** Minimal unit of PRD "firewall/access settings" — whether the service is publicly reachable. */
public enum NetworkAccess {
    PUBLIC,
    PRIVATE
}
