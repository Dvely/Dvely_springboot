-- 어시스턴트 메시지의 종류. 화면이 본문 문자열로 의미를 추론하지 않게 한다.
--
-- 그 추론은 이미 한 번 사고를 냈다 — 본문에 "승인 후 실행" 이 들어 있으면 승인 버튼을 붙이던
-- 시절, 모델이 그 문장을 지어내자 존재하지 않는 버튼이 떴다. 서버가 문구를 한 글자 다듬는
-- 것만으로도 조용히 깨지는 결합이다.
--
-- NULL 허용이다: 이 칼럼 이전에 쌓인 줄과, 순수 대화 답변처럼 종류를 붙일 이유가 없는 줄이 있다.
ALTER TABLE chat_messages
    ADD COLUMN kind VARCHAR(40) NULL AFTER content;
